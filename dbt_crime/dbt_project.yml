name: 'dbt_crime'
version: '1.0.0'
config-version: 2

profile: 'dbt_crime'

model-paths: ["models"]
test-paths: ["tests"]
macro-paths: ["macros"]
target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  dbt_crime:
    staging:
      +materialized: view
      +schema: staging
    marts:
      +materialized: table
      +schema: marts
